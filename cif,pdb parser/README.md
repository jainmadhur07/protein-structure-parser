## Protein Structure Parser

Parses PDB and CIF files using Biopython.
Handles duplicate chain IDs by renaming them (A1, A2, etc.)
Prints chain ID, residue name, atom name, and coordinates for each atom.

### Usage
Place your .pdb or .cif file in the same directory and run:
    python parser.py